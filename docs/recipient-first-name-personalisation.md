# Recipient first-name personalisation

Production campaign sends must render independently for each recipient. `{{first_name}}` uses the trimmed `marketing_contacts.first_name` value and falls back to `there`. Designer sample data is never valid for provider submission.
