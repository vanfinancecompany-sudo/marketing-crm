import fs from "node:fs";
import { fileURLToPath } from "node:url";

function replaceOnce(source, before, after, label) {
  if (source.includes("VFC_CREATE_OR_UPDATE_RECONCILE")) return source;
  const first = source.indexOf(before);
  if (first === -1) throw new Error(`Photo-ready detail repair could not find ${label}.`);
  if (source.indexOf(before, first + before.length) !== -1) throw new Error(`Photo-ready detail repair found duplicate ${label}.`);
  return source.replace(before, after);
}

const controlledTestPath = fileURLToPath(new URL("../tests/dealerkit-controlled-publish.test.js", import.meta.url));
let controlledTestSource = fs.readFileSync(controlledTestPath, "utf8");

if (!controlledTestSource.includes('["publish_new_vehicle", "update_existing_vehicle"].includes(action)')
  && !controlledTestSource.includes('final publisher requires confirmation, reconciles rows and publish status')) {
  const beforeTitle = 'test("final publisher requires confirmation, inserts only new rows, verifies and rolls back", async () => {';
  const afterTitle = 'test("final publisher requires confirmation, supports safe create or image-only update, verifies and rolls back", async () => {';
  const beforeAssertion = '  assert.match(source, /action !== "publish_new_vehicle"/);';
  const afterAssertion = '  assert.match(source, /\\["publish_new_vehicle", "update_existing_vehicle"\\]\\.includes\\(action\\)/);';
  if (!controlledTestSource.includes(beforeTitle) || !controlledTestSource.includes(beforeAssertion)) throw new Error("Controlled publish test contract anchors changed.");
  controlledTestSource = controlledTestSource.replace(beforeTitle, afterTitle).replace(beforeAssertion, afterAssertion);
  fs.writeFileSync(controlledTestPath, controlledTestSource);
}

const statePath = fileURLToPath(new URL("../api/_dealerkit-controlled-publish-state.js", import.meta.url));
let stateSource = fs.readFileSync(statePath, "utf8");
const variantImport = 'import { registrationTitleVariants } from "../lib/wixRegistrationVariants.js";';
if (!stateSource.includes(variantImport)) {
  const importAnchor = 'import { buildControlledPublishConfirmation, buildControlledVehiclePublishPlan } from "../lib/dealerKitControlledPublishPlan.js";';
  if (!stateSource.includes(importAnchor)) throw new Error("DealerKit detail lookup import anchor changed.");
  stateSource = stateSource.replace(importAnchor, `${importAnchor}\n${variantImport}`);
}

if (!stateSource.includes('const candidates = detailCollection ? registrationTitleVariants(registration) : [registration];')) {
  const startMarker = 'async function queryRegistration(configuration, collectionId, registration, collection = null) {';
  const endMarker = '\nasync function verifyManualRow(';
  const start = stateSource.indexOf(startMarker);
  const end = stateSource.indexOf(endMarker, start);
  if (start === -1 || end === -1) throw new Error("DealerKit detail registration lookup function anchors changed.");
  const replacement = `async function queryRegistration(configuration, collectionId, registration, collection = null) {\n  const detailCollection = collectionId === "VANFINANCEPAGES" || collectionId === "VANPAGES";\n  const candidates = detailCollection ? registrationTitleVariants(registration) : [registration];\n  const matched = new Map();\n\n  for (const candidate of candidates) {\n    const payload = await controlledWixRequest(configuration, "/wix-data/v2/items/query", {\n      method: "POST",\n      body: { dataCollectionId: collectionId, query: { filter: { title: { $eq: candidate } }, paging: { limit: 3, offset: 0 } }, consistentRead: true },\n    });\n    for (const item of Array.isArray(payload.dataItems) ? payload.dataItems : []) {\n      if (normalizeFinanceRegistration(item?.data?.title || "") !== registration) continue;\n      const id = clean(item?.id, 300);\n      if (id) matched.set(id, item);\n    }\n    if (!detailCollection && matched.size) break;\n  }\n\n  return {\n    siteId: configuration.siteId,\n    siteLabel: configuration.siteLabel || null,\n    siteRole: configuration.siteRole || null,\n    collectionId,\n    collection: collection || { id: collectionId },\n    items: Array.from(matched.values()),\n  };\n}\n`;
  stateSource = `${stateSource.slice(0, start)}${replacement}${stateSource.slice(end)}`;
  fs.writeFileSync(statePath, stateSource);
}

// Photo-ready repair rule: an existing listing with no detail row is a valid due-in state.
// Update the verified listing image(s) and create only the missing detail row with the full reviewed gallery.
const plannerPath = fileURLToPath(new URL("../lib/dealerKitControlledPublishPlan.js", import.meta.url));
let plannerSource = fs.readFileSync(plannerPath, "utf8");

const financeBlockersBefore = `    if (!targets.some((target) => target.collectionId === "VANFINANCE-ALLVANS")) blockers.push({ code: "vfc_refresh_master_missing", message: "The existing Van Finance master listing could not be verified for this registration." });\n    if (!targets.some((target) => target.collectionId === "VANFINANCEPAGES")) blockers.push({ code: "vfc_refresh_detail_missing", message: "The existing Van Finance vehicle page could not be verified for this registration." });`;
const financeBlockersAfter = `    const detailEntry = byCollection.get("VANFINANCEPAGES") || {};\n    const detailItems = existingItems(detailEntry);\n    if (detailItems.length === 0) {\n      const collection = detailEntry.collection || { id: "VANFINANCEPAGES", kind: "detail" };\n      const create = buildDealerKitWixCreatePlan({ collection, vehicle, decision, selectedImages: { ids: imageSets.dealerKitImageIds || [], count: imageSets.dealerKitImageIds?.length || 0 } });\n      for (const item of create.blockers || []) blockers.push({ code: \`vfc_\${item.code}\`, message: \`VANFINANCEPAGES: \${item.message}\` });\n      const data = { ...(create.proposedFields || {}) };\n      data.mainImages = [...(images.galleryUrls || [])];\n      data.descriptionLine = clean(vehicle.attentionGrabber, 1000) || clean(vehicle.title, 1000);\n      data.vehicleDescriptionTextClick = vfcDescription(vehicle);\n      data.imageCount = String(images.galleryUrls?.length || 0);\n      targets.push({ collectionId: "VANFINANCEPAGES", kind: "detail", operation: "create", data });\n    }\n\n    if (!targets.some((target) => target.collectionId === "VANFINANCE-ALLVANS")) blockers.push({ code: "vfc_refresh_master_missing", message: "The existing Van Finance master listing could not be verified for this registration." });\n    if (!targets.some((target) => target.collectionId === "VANFINANCEPAGES")) blockers.push({ code: "vfc_refresh_detail_unavailable", message: "The Van Finance vehicle page could not be safely updated or created for this registration." });`;
plannerSource = replaceOnce(plannerSource, financeBlockersBefore, financeBlockersAfter, "Finance missing-detail repair anchor");

const rentBefore = `        const matchingSiteTargets = rent2buyTargets.filter((target) => clean(target.siteId, 500) === site.siteId || (sites.length === 1 && !site.siteId));\n        if (!matchingSiteTargets.some((target) => target.collectionId === "ALLRENT2BUYVANS")) blockers.push({ code: "rent2buy_refresh_master_missing", message: \`\${siteLabel} / ALLRENT2BUYVANS could not be verified for \${registration}.\` });\n        if (!matchingSiteTargets.some((target) => target.collectionId === "VANPAGES")) blockers.push({ code: "rent2buy_refresh_detail_missing", message: \`\${siteLabel} / VANPAGES could not be verified for \${registration}.\` });\n        continue;`;
const rentAfter = `        const detailItems = existingItems(resultByCollection.get("VANPAGES"));\n        if (detailItems.length === 0) {\n          const plannedDetail = siteTargets.find((target) => target.collectionId === "VANPAGES");\n          if (plannedDetail) rent2buyTargets.push({ ...plannedDetail, operation: "create" });\n          else blockers.push({ code: "rent2buy_refresh_detail_unavailable", message: \`\${siteLabel} / VANPAGES could not be safely prepared for \${registration}.\` });\n        }\n        const matchingSiteTargets = rent2buyTargets.filter((target) => clean(target.siteId, 500) === site.siteId || (sites.length === 1 && !site.siteId));\n        if (!matchingSiteTargets.some((target) => target.collectionId === "ALLRENT2BUYVANS")) blockers.push({ code: "rent2buy_refresh_master_missing", message: \`\${siteLabel} / ALLRENT2BUYVANS could not be verified for \${registration}.\` });\n        if (!matchingSiteTargets.some((target) => target.collectionId === "VANPAGES")) blockers.push({ code: "rent2buy_refresh_detail_unavailable", message: \`\${siteLabel} / VANPAGES could not be safely updated or created for \${registration}.\` });\n        continue;`;
plannerSource = replaceOnce(plannerSource, rentBefore, rentAfter, "Rent2Buy missing-detail repair anchor");

const mixedBefore = `  const hasCreateTargets = targets.some((target) => target.operation !== "update");\n  const hasUpdateTargets = targets.some((target) => target.operation === "update");\n  const mixedWriteIntent = hasCreateTargets && hasUpdateTargets;\n  if (mixedWriteIntent) blockers.push({ code: "mixed_write_intent", message: "Creating a new vehicle and refreshing an existing vehicle cannot be combined in one controlled write. Run each product separately." });\n  const writeIntent = hasUpdateTargets && !hasCreateTargets ? "update_existing_vehicle" : "create_new_vehicle";`;
const mixedAfter = `  const hasCreateTargets = targets.some((target) => target.operation !== "update");\n  const hasUpdateTargets = targets.some((target) => target.operation === "update");\n  const createTargets = targets.filter((target) => target.operation !== "update");\n  const photoReadyDetailRepair = hasUpdateTargets && createTargets.length > 0 && createTargets.every((target) => target.kind === "detail");\n  const mixedWriteIntent = hasCreateTargets && hasUpdateTargets && !photoReadyDetailRepair;\n  if (mixedWriteIntent) blockers.push({ code: "mixed_write_intent", message: "Creating a new vehicle and refreshing an existing vehicle cannot be combined in one controlled write unless the only new row is a missing photo-ready detail page." });\n  const writeIntent = hasUpdateTargets && (!hasCreateTargets || photoReadyDetailRepair) ? "update_existing_vehicle" : "create_new_vehicle";`;
plannerSource = replaceOnce(plannerSource, mixedBefore, mixedAfter, "photo-ready mixed write intent");
fs.writeFileSync(plannerPath, plannerSource);

// Cars use a separate planner/API. Apply the same listing-exists/detail-missing repair rule there.
const carPlanPath = fileURLToPath(new URL("../lib/dealerKitCarWixPlan.js", import.meta.url));
let carPlanSource = fs.readFileSync(carPlanPath, "utf8");
const carStart = carPlanSource.indexOf("export function buildDealerKitCarWixPlan(");
const carEnd = carPlanSource.indexOf("\nfunction stable(", carStart);
if (carStart === -1 || carEnd === -1) throw new Error("Cars planner anchors changed.");
const carReplacement = `function carMediaUpdateTarget(target, existingItem, data) {\n  return { ...target, operation: "update", itemId: clean(existingItem?.id, 300), previousData: existingItem?.data || {}, data };\n}\n\nexport function buildDealerKitCarWixPlan({ vehicle = {}, decision = {}, imageSet = {}, carListingRows = [], carDetailRows = [] } = {}) {\n  const registration = normalizeFinanceRegistration(vehicle.registration || decision.registration || "");\n  const retailPrice = parseRetailPrice(vehicle.retailPrice);\n  const monthlyPrice = calculateFivePercentFlatMonthly(retailPrice);\n  const blockers = [];\n  const listingRows = Array.isArray(carListingRows) ? carListingRows : [];\n  const detailRows = Array.isArray(carDetailRows) ? carDetailRows : [];\n  const refreshExisting = listingRows.length === 1;\n\n  if (!registration) blockers.push({ code: "missing_registration", message: "A valid registration is required for Cars publishing." });\n  if (!decision.persisted || decision.reviewStatus !== "reviewed") blockers.push({ code: "review_not_approved", message: "Save the Cars review before publishing." });\n  if (vehicle.sourceUpdatedAt && decision.reviewedSourceUpdatedAt && new Date(vehicle.sourceUpdatedAt).getTime() !== new Date(decision.reviewedSourceUpdatedAt).getTime()) blockers.push({ code: "stale_review", message: "DealerKit changed after the saved Cars review. Re-save the review first." });\n  if (retailPrice === null || monthlyPrice === null) blockers.push({ code: "invalid_retail", message: "A valid DealerKit retail price is required." });\n  if (!["available", "in_stock", "due_in"].includes(clean(vehicle.status || vehicle.sourceStatus, 100).toLowerCase().replace(/[\\s-]+/g, "_"))) blockers.push({ code: "source_status", message: "DealerKit no longer shows this car as available for advertising." });\n  if (!imageSet.ready || !imageSet.mainUrl || !imageSet.galleryUrls?.length) blockers.push({ code: "car_media_not_ready", message: "Cars primary image is not READY in Wix Media." });\n  if (listingRows.length > 1) blockers.push({ code: "car_listing_ambiguous", message: \`CARFINANCE contains \${listingRows.length} published listing rows for \${registration}. Resolve the duplicate before updating images.\` });\n  if (detailRows.length > 1) blockers.push({ code: "car_detail_ambiguous", message: \`CARPAGES contains \${detailRows.length} published rows for \${registration}. Resolve the duplicate detail pages before publishing.\` });\n\n  const sections = featureSections(vehicle);\n  const description = clean(vehicle.description, 10000) || headline(vehicle);\n  const listingData = registration && retailPrice !== null && monthlyPrice !== null && imageSet.mainUrl ? {\n    title: registration, picture: imageSet.mainUrl, price: formatRetailPrice(retailPrice), salePrice: \`FROM £\${monthlyPrice} P/M\`,\n    vanDescription: headline(vehicle), vanSpec: listingSpec(vehicle, registration),\n    webLink: \`https://www.vanfinancecompany.co.uk/van-finance/\${registration}\`, buttonText: "VIEW CAR",\n    applicationForm: \`https://www.vanfinancecompany.co.uk/apply-by-reg-finance/\${registration}\`,\n  } : null;\n  const detailData = registration && retailPrice !== null && monthlyPrice !== null && imageSet.galleryUrls?.length ? {\n    title: registration, titleText: headline(vehicle), priceVat: formatRetailPrice(retailPrice), year: displayYear(vehicle, registration),\n    vanFinanceTitle: description, mainImages: imageSet.galleryUrls, backLink: "https://www.vanfinancecompany.co.uk/cars",\n    descriptionLine: detailSpec(vehicle, registration), vehicleDescriptionTextClick: description, vehicleSpecificationText: allFeatures(vehicle).join("\\n"),\n    applyLink: \`https://www.vanfinancecompany.co.uk/apply-by-reg-finance/\${registration}\`,\n    audioAndCommunications: [keyVehicleInformation(vehicle), sections.audioAndCommunications].filter(Boolean).join("\\n\\n"),\n    driversAssistance: sections.driversAssistance, exterior: sections.exterior, illumination: sections.illumination, interior: sections.interior,\n    performance: sections.performance, safetyAndSecurity: sections.safetyAndSecurity, salePrice: \`£\${monthlyPrice}\`, numberOfImages: String(imageSet.galleryUrls.length),\n  } : null;\n\n  const targets = [];\n  if (listingData) {\n    if (refreshExisting) targets.push(carMediaUpdateTarget({ product: "cars", collectionId: "CARFINANCE", kind: "listing" }, listingRows[0], { picture: imageSet.mainUrl }));\n    else targets.push({ product: "cars", collectionId: "CARFINANCE", kind: "listing", operation: "create", data: listingData });\n  }\n  if (detailData) {\n    const existing = detailRows[0] || null;\n    if (refreshExisting && existing?.id) targets.push(carMediaUpdateTarget({ product: "cars", collectionId: "CARPAGES", kind: "detail" }, existing, { mainImages: imageSet.galleryUrls, numberOfImages: String(imageSet.galleryUrls.length) }));\n    else if (refreshExisting) targets.push({ product: "cars", collectionId: "CARPAGES", kind: "detail", operation: "create", data: detailData });\n    else targets.push({ product: "cars", collectionId: "CARPAGES", kind: "detail", operation: existing?.id ? "update" : "create", ...(existing?.id ? { itemId: existing.id, previousData: existing.data || {} } : {}), data: detailData });\n  }\n\n  return {\n    version: 1, mode: "cars", registration, retailPrice, monthlyPrice, imageSets: { cars: imageSet }, targets, blockers,\n    writeIntent: refreshExisting ? "update_existing_vehicle" : "create_new_vehicle",\n    canPublish: blockers.length === 0 && targets.some((target) => target.collectionId === "CARFINANCE") && targets.some((target) => target.collectionId === "CARPAGES"),\n  };\n}\n`;
carPlanSource = `${carPlanSource.slice(0, carStart)}${carReplacement}${carPlanSource.slice(carEnd)}`;
carPlanSource = replaceOnce(
  carPlanSource,
  `    mode: "cars",\n    registration: plan.registration,`,
  `    mode: "cars",\n    writeIntent: plan.writeIntent || "create_new_vehicle",\n    registration: plan.registration,`,
  "Cars confirmation write intent",
);
fs.writeFileSync(carPlanPath, carPlanSource);

const carApiPath = fileURLToPath(new URL("../api/dealerkit-car-controlled-publish.js", import.meta.url));
let carApiSource = fs.readFileSync(carApiPath, "utf8");
carApiSource = replaceOnce(
  carApiSource,
  `  if (clean(request.body?.action, 100) !== "publish_new_car") return response.status(400).json({ ok: false, message: "Controlled Cars publish action is not supported." });`,
  `  const action = clean(request.body?.action, 100);\n  if (!["publish_new_car", "update_existing_car"].includes(action)) return response.status(400).json({ ok: false, message: "Controlled Cars publish action is not supported." });`,
  "Cars accepted action",
);
carApiSource = replaceOnce(
  carApiSource,
  `    state = await buildFreshCarControlledPublishState(registration);\n    if (!state.plan.canPublish) throw new ControlledPublishError(409, "The fresh DealerKit/Wix Cars state is not safe for publishing.", { blockers: state.plan.blockers });`,
  `    state = await buildFreshCarControlledPublishState(registration);\n    const requestedIntent = action === "update_existing_car" ? "update_existing_vehicle" : "create_new_vehicle";\n    if (state.plan.writeIntent !== requestedIntent) throw new ControlledPublishError(409, "The Cars Wix write intent changed during the final recheck. Rebuild the preview before continuing.", { expected: state.plan.writeIntent, requested: requestedIntent });\n    if (!state.plan.canPublish) throw new ControlledPublishError(409, "The fresh DealerKit/Wix Cars state is not safe for publishing.", { blockers: state.plan.blockers });`,
  "Cars write intent recheck",
);
fs.writeFileSync(carApiPath, carApiSource);

const uiPath = fileURLToPath(new URL("../utils/dealerKitControlledPublish.js", import.meta.url));
let uiSource = fs.readFileSync(uiPath, "utf8");
uiSource = replaceOnce(
  uiSource,
  `        ? { action: "publish_new_car", registration, confirmRegistration: normaliseRegistration(input.value), confirmation: payload.plan.confirmation }`,
  `        ? { action: updateExisting ? "update_existing_car" : "publish_new_car", registration, confirmRegistration: normaliseRegistration(input.value), confirmation: payload.plan.confirmation }`,
  "Cars UI update action",
);
fs.writeFileSync(uiPath, uiSource);

const mediaTestPath = fileURLToPath(new URL("../tests/dealerkit-existing-media-refresh.test.js", import.meta.url));
let mediaTestSource = fs.readFileSync(mediaTestPath, "utf8");
const testMarker = 'test("spaced Wix detail registrations are found before the gallery refresh is allowed"';
if (!mediaTestSource.includes(testMarker)) {
  mediaTestSource += `\n\ntest("spaced Wix detail registrations are found before the gallery refresh is allowed", async () => {\n  const { registrationTitleVariants } = await import("../lib/wixRegistrationVariants.js");\n  assert.ok(registrationTitleVariants("BD21HCX").includes("BD21 HCX"));\n  const stateSource = fs.readFileSync(new URL("../api/_dealerkit-controlled-publish-state.js", import.meta.url), "utf8");\n  assert.match(stateSource, /registrationTitleVariants\\(registration\\)/);\n  assert.match(stateSource, /normalizeFinanceRegistration\\(item\\?\\.data\\?\\.title \\|\\| ""\\) !== registration/);\n});\n`;
}

if (!mediaTestSource.includes('test("existing Finance listing creates the missing detail page during photo-ready repair"')) {
  mediaTestSource += `\n\ntest("existing Finance listing creates the missing detail page during photo-ready repair", () => {\n  const wixResults = financeResults().map((entry) => entry.collectionId === "VANFINANCEPAGES" ? { ...entry, items: [] } : entry);\n  const plan = buildControlledVfcTargets({ vehicle: vehicle(), decision: decision(), imageSets: imageSets(), wixResults });\n  assert.equal(plan.writeIntent, "update_existing_vehicle");\n  assert.equal(plan.canPublish, true);\n  const detail = plan.targets.find((target) => target.collectionId === "VANFINANCEPAGES");\n  assert.equal(detail.operation, "create");\n  assert.deepEqual(detail.data.mainImages, galleryUrls);\n  assert.equal(detail.data.imageCount, "3");\n  assert.ok(plan.targets.filter((target) => target.kind === "listing").every((target) => target.operation === "update"));\n});\n\ntest("Rent2Buy photo-ready repair creates only a missing VANPAGES detail row", () => {\n  const rentDecision = { ...decision(), financeEnabled: false, rent2buyEnabled: true, rent2buyCategories: ["medium"], financeCategories: [], };\n  const rentVehicle = { ...vehicle(), mileage: 42000, vatStatus: "plus_vat" };\n  const rentImages = { ...imageSets(), vanFinance: {}, rent2buy: { ready: true, mainUrl: listingImageUrl, listingImageUrl, galleryUrls, mainSource: "dealerkit_primary" } };\n  const site = { siteId: "85f11c52-ee54-495d-aaec-a351831709b5", siteLabel: "Van Finance Rent2Buy", siteRole: "primary" };\n  const rentResults = [\n    { ...site, collectionId: "ALLRENT2BUYVANS", collection: { id: "ALLRENT2BUYVANS", kind: "listing" }, items: [item("r2b-all", { picture: "old.jpg" })] },\n    { ...site, collectionId: "MEDIUMVANS", collection: { id: "MEDIUMVANS", kind: "listing" }, items: [item("r2b-medium", { picture: "old.jpg" })] },\n    { ...site, collectionId: "VANPAGES", collection: { id: "VANPAGES", kind: "detail" }, items: [] },\n  ];\n  const plan = buildControlledVehiclePublishPlan({ vehicle: rentVehicle, decision: rentDecision, imageSets: rentImages, vfcWixResults: [], rent2buyWixResults: rentResults, rent2buySites: [site], productMode: "rent2buy" });\n  assert.equal(plan.writeIntent, "update_existing_vehicle");\n  assert.equal(plan.canPublish, true);\n  const detail = plan.targets.find((target) => target.collectionId === "VANPAGES");\n  assert.equal(detail.operation, "create");\n  assert.deepEqual(detail.data.mediaGallery, galleryUrls);\n  assert.ok(plan.targets.filter((target) => target.kind === "listing").every((target) => target.operation === "update"));\n});\n\ntest("Cars photo-ready repair updates the existing listing and creates a missing CARPAGES detail row", async () => {\n  const { buildDealerKitCarWixPlan } = await import("../lib/dealerKitCarWixPlan.js");\n  const carVehicle = { ...vehicle(), status: "available", mileage: 42000, year: 2022, make: "Ford", model: "Focus", description: "Ford Focus", specifications: { technical: [], standard: [], options: [] } };\n  const carDecision = { registration, persisted: true, reviewStatus: "reviewed", reviewedSourceUpdatedAt: carVehicle.sourceUpdatedAt };\n  const carImages = { ready: true, mainUrl: listingImageUrl, listingImageUrl, galleryUrls, dealerKitImageIds: ["dk-1", "dk-2", "dk-3"] };\n  const plan = buildDealerKitCarWixPlan({ vehicle: carVehicle, decision: carDecision, imageSet: carImages, carListingRows: [item("car-list", { picture: "old.jpg", price: "£13,995" })], carDetailRows: [] });\n  assert.equal(plan.writeIntent, "update_existing_vehicle");\n  assert.equal(plan.canPublish, true);\n  const listing = plan.targets.find((target) => target.collectionId === "CARFINANCE");\n  const detail = plan.targets.find((target) => target.collectionId === "CARPAGES");\n  assert.equal(listing.operation, "update");\n  assert.deepEqual(listing.data, { picture: listingImageUrl });\n  assert.equal(detail.operation, "create");\n  assert.deepEqual(detail.data.mainImages, galleryUrls);\n});\n`;
}
fs.writeFileSync(mediaTestPath, mediaTestSource);

console.log("Updated controlled publish contract: photo-ready listings may safely create a missing detail page across Finance, Rent2Buy and Cars.");

// This is the final Stock Watch page transform so source-truth gates cannot be
// weakened by an earlier UI compatibility transform.
await import("./apply-dealerkit-stock-truth-safety.mjs");
